apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kube-oidc-fed-broker
  namespace: kube-oidc-fed
spec:
  selector:
    matchLabels:
      app: kube-oidc-fed-broker
  template:
    metadata:
      labels:
        app: kube-oidc-fed-broker
    spec:
      serviceAccountName: kube-oidc-fed-broker
      containers:
      - name: broker
        image: kube-oidc-fed-broker:latest
        ports:
        - containerPort: 8080
        env:
        - name: BROKER_ISSUER
          valueFrom:
            configMapKeyRef:
              name: kube-oidc-fed-broker-config
              key: issuer
        - name: BROKER_REGISTRY_URL
          valueFrom:
            configMapKeyRef:
              name: kube-oidc-fed-broker-config
              key: registry_url
        - name: BROKER_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: kube-oidc-fed-broker-auth
              key: token
        - name: BROKER_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
