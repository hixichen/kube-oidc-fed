apiVersion: v1
kind: Namespace
metadata:
  name: kube-oidc-fed
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kube-oidc-fed-registry
  namespace: kube-oidc-fed
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-oidc-fed-registry
  namespace: kube-oidc-fed
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kube-oidc-fed-registry
  template:
    metadata:
      labels:
        app: kube-oidc-fed-registry
    spec:
      serviceAccountName: kube-oidc-fed-registry
      containers:
      - name: registry
        image: kube-oidc-fed-registry:latest
        ports:
        - containerPort: 8080
        env:
        - name: REGISTRY_S3_BUCKET
          valueFrom:
            configMapKeyRef:
              name: kube-oidc-fed-config
              key: s3_bucket
        - name: REGISTRY_S3_REGION
          valueFrom:
            configMapKeyRef:
              name: kube-oidc-fed-config
              key: s3_region
        - name: REGISTRY_ISSUER
          valueFrom:
            configMapKeyRef:
              name: kube-oidc-fed-config
              key: issuer
        - name: REGISTRY_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: kube-oidc-fed-auth
              key: token
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: kube-oidc-fed-registry
  namespace: kube-oidc-fed
spec:
  selector:
    app: kube-oidc-fed-registry
  ports:
  - port: 80
    targetPort: 8080
